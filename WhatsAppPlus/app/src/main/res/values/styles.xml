<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorControlNormal">@color/color_add_button</item>
        <item name="colorControlActivated">@color/color_add_button</item>
        <item name="colorControlHighlight">@color/color_add_button</item>

        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:editTextColor">@color/color_chat_message_input</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>

    <style name="SelectionToolbarTheme" parent="AppTheme">
        <item name="android:textColorPrimary">@color/colorAccent</item>

        <item name="colorPrimary">@color/color_toolbar_selection_primary</item>
        <item name="colorPrimaryDark">@color/color_toolbar_selection_primary_dark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/color_toolbar_selection_highlight</item>
    </style>

    <style name="ToolbarTheme" parent="AppTheme">
        <item name="android:textColorPrimary">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorAccent</item>
    </style>

    <style name="ToolbarPopupTheme" parent="AppTheme">
        <item name="android:textColorPrimary">@android:color/black</item>
    </style>

</resources>
